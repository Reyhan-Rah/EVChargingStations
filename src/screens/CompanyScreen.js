import React from 'react';
import { View, Text, StyleSheet } from 'react-native';

const CompanyScreen = () => {
    return (
    <Text style={{ fontSize: 48 }}>CompanyScreen</Text>
    );
}

const styles = StyleSheet.create({});

export default CompanyScreen;