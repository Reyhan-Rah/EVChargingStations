import React from 'react';
import { View, Text, StyleSheet } from 'react-native';

const StationsScreen = () => {
    return <Text style={{ fontSize: 48 }}>StationsScreen</Text>;
};

const styles = StyleSheet.create({});

export default StationsScreen;
